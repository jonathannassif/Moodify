JSON 
Events → Async → 
Error Handling → ES6+ Features → Modules →
Storage →OOP





7️⃣ Filter/Search Items

Input field for search

As you type → filter list items (hide ones that don’t match)

Practice: .forEach, .includes(), .style.display

🌐 LEVEL 3 — Practical “Job Simulation” Projects
8️⃣ Expense Tracker (Mini Dashboard)

Inputs: description, amount

Add button → adds to list

Show total spent dynamically

Practice: arrays + objects + DOM rendering

9️⃣ Form Validation System

Inputs: name, email, password

On submit → check for:

empty fields

email pattern

password length

Display error/success messages under each field

Practice: form events, validation, .preventDefault()

🔟 Simple Image Gallery

Show small preview images

Click → opens larger version in modal/lightbox

Add “next” and “previous” buttons

Practice: event listeners, dynamic content, CSS classe
