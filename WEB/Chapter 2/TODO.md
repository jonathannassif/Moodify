### 🔥 **Level 4 – Deep Practice**

9. **Polymorphism challenge**

   - Create a list of different vehicles.
   - Loop through them and call `drive()` → each prints a unique message.
   - Understand how the same method name behaves differently.

10. **Abstract-style exercise (no true abstract in JS but simulate it)**

- Create a base class `Shape` with method `area()` that throws an error.
- Subclasses: `Circle`, `Square`
- Each implements its own `area()` logic.

---

### 🧠 **Optional Brain Stretch**

11. **Chained inheritance**

- `Person` → `Employee` → `Manager`
- Each adds one property and method.
- Use `super()` in each and override one method in `Manager`.

12. **Mix parent and child logic**

- In `Manager`, override a parent method but also call it with `super.method()` first before adding more logic.

---

If you want, I can turn these into a **7-day challenge plan** (each day focusing on one theme with hints + goals) — would you like me to do that?
